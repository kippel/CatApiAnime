services:
  anime:
    build: ./anime
    ports:
      - "4000:4000"
    volumes:
      - ./anime/data:/app/data
    environment:
      - SQLITE=true
      - DATABASE_URL=sqlite:////app/data/sql_app_test.db
